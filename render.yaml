services:
  - type: web
    name: weddingplanner-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
    repo: https://github.com/GuZzKiR/WeddingPlannerPro_Backend
    branch: main
    region: oregon
    plan: free